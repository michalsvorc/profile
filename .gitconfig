#===============================================================================
# User
#===============================================================================

[user]
  email = dev@michalsvorc.com
  name = Michal Svorc

#===============================================================================
# Aliases
#===============================================================================

[alias]
  c  = commit
  cc = checkout
  cl = clone
  d  = diff
  ds = diff --staged
  dt = difftool
  l  = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr: %cd) %C(bold blue)<%ae>%Creset' --abbrev-commit --date=iso
  r  = restore
  rs = restore --staged
  s  = status --short
  sw = switch

#===============================================================================
# Main
#===============================================================================

[core]
  pager = bat --style=plain --paging=never --language=diff
  editor = nvim

[init]
  defaultBranch = main

[pull]
  rebase = false

[help]
  autocorrect = 30

[commit]
  verbose = true

#===============================================================================
# Diff
#===============================================================================

[diff]
  tool = nvim_diff
  colorMoved = default
  colorMoved = zebra
  colorMovedWS = allow-indentation-change
  algorithm = histogram
  indentHeuristic = true
  context = 5
  interHunkContext = 3
  compactionHeuristic = true
  renameLimit = 9999
  wordRegex = [^[:space:]]+

[color "diff"]
  oldMoved = red bold
  newMoved = green bold

[difftool]
  prompt = false
  colorMoved = default

[difftool "nvim_diff"]
  cmd = nvim -d "$LOCAL" "$REMOTE"

#===============================================================================
# Merge
#===============================================================================

[merge]
  tool = nvim_merge
  conflictstyle = diff3

[mergetool]
  prompt = false
  keepBackup = false
  trustExitCode = false

[mergetool "nvim_merge"]
  cmd = nvim -d "$LOCAL" "$REMOTE" "$MERGED"
